

import 'package:flutter/material.dart';

class ItemPageMobile extends StatelessWidget{
  const ItemPageMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue,
    child: Text('Mobile'),);
  }
}